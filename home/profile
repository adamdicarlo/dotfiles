# ~/.profile: executed by the command interpreter for login shells.
# This file is not read by bash(1), if ~/.bash_profile or ~/.bash_login
# exists.
# see /usr/share/doc/bash/examples/startup-files for examples.
# the files are located in the bash-doc package.

# the default umask is set in /etc/profile; for setting the umask
# for ssh logins, install and configure the libpam-umask package.
#umask 022

# if running bash
if [ -n "$BASH_VERSION" ]; then
    # include .bashrc if it exists
    if [ -f "$HOME/.bashrc" ]; then
        . "$HOME/.bashrc"
    fi
    export HISTCONTROL=ignoredups
    shopt -s histappend
    PROMPT_COMMAND='history -a'
    if [ -f "$HOME/.bash_aliases" ]; then
        . "$HOME/.bash_aliases"
    fi
fi

export CVSROOT=:pserver:bikko@cvs.drupal.org:/cvs/drupal-contrib
export EDITOR=vim
export VISUAL=vim

if [ "$(/bin/hostname)" == "skippy.local" ]; then
  # MacBook
  echo Skippy
  export PS1='\[\033[01;32m\]\u@\h \[\033[01;34m\]\w\[\033[00m\]$ '
  alias ls='ls -F --color=auto'
  alias ll='ls -lagh'
  alias hibernate_on='sudo pmset -a hibernatemode 5'
  alias hibernate_off='sudo pmset -a hibernatemode 3'
  alias photoshop='open -a "Adobe Photoshop CS4"'

  export PATH=$HOME/.drush/drush:/opt/local/bin:/usr/local/bin:/usr/local/git/bin:/opt/local/sbin:~/bin:$PATH:/Applications/MAMP/Library/bin
  export MANPATH=/opt/local/share/man:/usr/local/share/man:$MANPATH:/Library/Framework/Mono.framework/Versions/Current/man

else
  # Linux
  export PATH="$HOME/bin:$HOME/.drush/drush:/var/lib/gems/1.8/bin:$PATH"
fi

